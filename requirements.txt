# Local VLM stack for the AnythingUse decision actor (Qwen3-VL on Apple Silicon).
# Python 3.11+; install into the project venv:
#   python3 -m venv .venv && .venv/bin/pip install -r requirements.txt
# Then fetch weights: ./scripts/download_qwen3_vl.sh
accelerate==1.14.0
huggingface_hub==1.24.0
numpy==2.5.1
pillow==12.3.0
regex==2026.7.19
requests==2.34.2
safetensors==0.8.0
tokenizers==0.22.2
torch==2.13.0
torchvision==0.28.0
tqdm==4.69.1
transformers==5.14.1
