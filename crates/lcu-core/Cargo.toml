[package]
name = "lcu-core"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "macOS layer over the shared AnythingUse core: re-exports anything-core and adds the mac evidence guard (StaticEffectGuard)"

[dependencies]
anything-core = { workspace = true }
serde = { workspace = true }
